.next/
node_modules/
data/editor/

# Local env files — never commit secrets. Covers .env, .env.local, and every
# per-environment local variant (.env.production.local, .env.development.local…).
.env
.env*.local

*.log
.DS_Store

# TypeScript incremental build cache — machine-specific, regenerated on every
# build. Should not be tracked.
tsconfig.tsbuildinfo

tribe-server/__pycache__/
tribe-server/.venv/
tribe-server/cache/
